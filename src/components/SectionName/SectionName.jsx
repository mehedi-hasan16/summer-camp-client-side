
const SectionName = ({title}) => {
    return (
        <div className="my-12">
            <h3 className="text-3xl font-bold uppercase text-center">{title}</h3>
        </div>
    );
};

export default SectionName;